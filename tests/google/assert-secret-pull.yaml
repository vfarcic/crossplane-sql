---
apiVersion: devopstoolkitseries.com/v1alpha1
kind: SQL
metadata:
  labels:
    crossplane.io/claim-name: my-db
spec:
  parameters:
    secrets:
      storeName: google
      pullRootPasswordKey: db-password
  (resourceRefs[?apiVersion == 'kubernetes.crossplane.io/v1alpha2' && kind == 'Object' && name == 'my-db-google-secret-pull']):
    (length(@)): 1
---
apiVersion: kubernetes.crossplane.io/v1alpha2
kind: Object
metadata:
  annotations:
    crossplane.io/composition-resource-name: my-db-google-secret-pull
    crossplane.io/external-name: my-db-google-secret-pull
  labels:
    crossplane.io/claim-name: my-db
  name: my-db-google-secret-pull
  ownerReferences:
  - apiVersion: devopstoolkitseries.com/v1alpha1
    blockOwnerDeletion: true
    controller: true
    kind: SQL
spec:
  deletionPolicy: Delete
  forProvider:
    manifest:
      apiVersion: external-secrets.io/v1beta1
      kind: ExternalSecret
      metadata:
        name: my-db-google-password
      spec:
        dataFrom:
        - extract:
            conversionStrategy: Default
            decodingStrategy: None
            key: db-password
            metadataPolicy: None
        refreshInterval: 1h
        secretStoreRef:
          kind: ClusterSecretStore
          name: google
        target:
          creationPolicy: Owner
          deletionPolicy: Retain
          name: my-db-google-password
  providerConfigRef:
    name: my-db-google-sql
