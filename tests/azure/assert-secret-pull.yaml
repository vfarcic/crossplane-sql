---
apiVersion: devopstoolkitseries.com/v1alpha1
kind: SQL
metadata:
  labels:
    crossplane.io/claim-name: my-db
spec:
  parameters:
    secrets:
      storeName: azure
      pullRootPasswordKey: db-password
  (resourceRefs[?name == 'my-db-azure-secret-pull']):
  - apiVersion: kubernetes.crossplane.io/v1alpha2
    kind: Object
---
apiVersion: kubernetes.crossplane.io/v1alpha2
kind: Object
metadata:
  annotations:
    crossplane.io/composition-resource-name: my-db-azure-secret-pull
    crossplane.io/external-name: my-db-azure-secret-pull
  labels:
    crossplane.io/claim-name: my-db
  name: my-db-azure-secret-pull
  ownerReferences:
  - apiVersion: devopstoolkitseries.com/v1alpha1
    blockOwnerDeletion: true
    controller: true
    kind: SQL
spec:
  deletionPolicy: Delete
  forProvider:
    manifest:
      apiVersion: external-secrets.io/v1beta1
      kind: ExternalSecret
      metadata:
        name: my-db-azure-password
      spec:
        dataFrom:
        - extract:
            conversionStrategy: Default
            decodingStrategy: None
            key: db-password
            metadataPolicy: None
        refreshInterval: 1h
        secretStoreRef:
          kind: ClusterSecretStore
          name: azure
        target:
          creationPolicy: Owner
          deletionPolicy: Retain
          name: my-db-azure-password
  providerConfigRef:
    name: my-db-azure-sql
